import InputButton from "./InputButton"

const Header = () => {
    return (
        <div className="header">
            <InputButton className="button-header">Sair</InputButton>
        </div>
    )
}

export default Header